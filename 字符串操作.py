# -*- coding: utf-8 -*-
"""
Created on Wed May  4 09:53:21 2022

@author: 15517
"""

lower()
upper()
len()
strip()
split('')           #用给定的模式生拆分每个字符串
cat(sep='')         #用给定的分隔符链接系列/索引因素
get_dummies()       #返回具有单热编码值得数据帧(DateFrame)
contains(pattern)   #如果元素中包含子字符串，则返回每个元素得布尔值
replace(a,b)        #将a替换为b
repeat(value)       #重复每个元素指定的次数
count(pattern)      #返回每个元素出现的次数
startswith()
endswith()
find(pattern)       #返回模式第一次出现的位置
findall(pattern)    #返回模式所有出现得列表
swapcase            #变换字母大小写
islower()
isupper()
isnumeric()